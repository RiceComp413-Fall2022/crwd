def report_n_devices(nDevices):
    '''Reports the given number of devices to the backend.'''
    print(f"Reporting nDevices ({nDevices}) to backend.")
    # TODO: call endpoint on backend: reportNDevices/nDevices
